{
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "PYTHONPATH": "."
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
